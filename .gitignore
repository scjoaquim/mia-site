# ── mia-site — o que NUNCA vai para o repo do site ────────────────────────────
# Criado 31-Jul-2026. Motivo: o repo nao tinha .gitignore nenhum, entao backups e
# sobras de ferramenta apareciam no GitHub Desktop como se fossem para commitar.
# Mesma convencao do repo da frota (AGENTE TRADE 1/.gitignore).

# Backups de edicao (o padrao do projeto e' *.bak_<motivo>_<data>)
*.bak
*.bak_*

# Sobras do lock do git: o bridge do Mac nao consegue apagar um .lock, so mover.
# A receita e' mover TODOS (index.lock, HEAD.lock, objects/maintenance.lock) para
# esta pasta antes de cada comando git. Sao ficheiros mortos - podem ser apagados
# a mao a qualquer momento.
_to_delete_git_lock/
_to_delete/

# Sistema
.DS_Store

# --- MODO MANUTENCAO 02-Ago-2026: fora do ar durante a revisao geral.
#     Arquivos preservados no disco e no git (branch site-completo-pre-manutencao,
#     tag snapshot-pre-manutencao-2026-08-02, commit c2f9a59cafac0ce09f30ad1b25a8206e6c8fde63).
#     Restaurar com: bash restaurar-site.sh
#
# --- [S4 - 12-Ago-2026] FIM DA MANUTENCAO. As paginas do site, o script.js e o
#     style.css SAIRAM desta lista e voltaram ao controlo de versoes. Estavam aqui
#     so enquanto o site estava fora do ar.
#     !! Mantidos, o defeito era silencioso: na raiz so o index.html e o 404.html
#     continuavam sob controlo de versoes, logo um `git add` NAO apanhava as outras
#     paginas nem o script.js - e o index.html novo subia a apontar para paginas
#     que nunca foram publicadas, e sem o script do dock. Nenhum erro a avisar.
#     !! O restaurar-site.sh continua a NAO se correr: ele traz o site de 02-Ago.
/data.json
/bots.json
/positions.json
/index.html.bak_13estrategias
/produtos.html.bak_13estrategias
/resultados.html.bak_nota_ranking_31jul
/ativar-manutencao.sh
/restaurar-site.sh
